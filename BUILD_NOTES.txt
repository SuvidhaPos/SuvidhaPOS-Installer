BUILD NOTES - V2.3 CLEAN UI

Source tree was rebuilt as a clean release tree. Obsolete UI patch scripts and the unused UiPolish.cs file are intentionally absent.

UI:
- Fixed header / sidebar / content / footer shell.
- Only pageBody is scrollable.
- Content host itself is not scrollable.
- Footer remains fixed and is never placed inside pageBody.
- Footer controls use a TableLayoutPanel rather than manual X/Y positioning.
- Next is wired directly and also assigned to the form AcceptButton.
- Per-monitor DPI scaling remains enabled.
- Minimum window size is 960x680 to keep the 7-step navigation and footer usable.

Software directory:
D:\Suvidha Pos\Software

No subfolders are created by installer download/check logic.

Installed installer application:
C:\Program Files\Suvidha Soft Installer

Build:
- .NET 8
- win-x64
- self-contained
- PublishSingleFile=false
- Inno Setup 6 packages the complete publish directory.

The GitHub workflow performs clean-tree, UI invariant, build, publish and final installer checks.
