Suvidha POS Installer V2 - Final Cross Check

Base: SuvidhaPOS-Installer-V2 SIDEBYSIDE-14001-FIX2 source tree.

Verified in this package:
- Complete source tree is included; no old partial 3-file patch is used.
- Premium 7-step WinForms UI and existing logo/assets are preserved.
- Responsive shell remains in MainForm.cs for 800x600 through large displays.
- Windows restart/resume task and resume.json state handling are preserved.
- SQL Server / SSMS / Crystal Reports component flow is preserved.
- SuvidhaPos.exe / RetailPos.exe config detection and SQL connection setup are preserved.
- Inno Setup copies the complete publish folder, not only one EXE.
- PublishSingleFile is explicitly disabled in the GitHub workflow.
- Microsoft.Data.SqlClient managed networking is enabled before the form starts.
- Workflow checks required files, manifest, EXE, DLL and logo assets before packaging.
- x64 self-contained .NET 8 publish is used.

Important:
- The GitHub Actions runner is Windows-only for the actual build.
- The final Setup EXE must be taken from the successful GitHub Actions artifact.
